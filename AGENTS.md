# Project working instructions

## Notion 기록 경로

- 이 프로젝트에서 수행하는 VOC 품질관리 단계별 구현·검증 기록은 Notion의 `VS Code > VOC_Improve > VOC 작업 기록` 데이터베이스에 새 항목으로 작성한다.
- `VOC_Improve` 인덱스 페이지 ID는 `3a76031e-8cac-8132-a68d-fb110ae6146b`이다.
- `VOC 작업 기록` 데이터 소스 ID는 `1cc3c477-ba4f-43c8-a689-391846a92d2d`이다.
- 장기 설계·운영 정책 결정은 `VS Code > VOC_Improve > 중요 설계 결정` 페이지에도 요약한다.
- 기존의 긴 기록은 `VS Code > VOC_Improve Archive · 2026-07-20 이전`에 보존하며 새로운 기록을 추가하지 않는다.
- 현재 프로젝트 폴더명과 같은 `VS Code > ai_quality_final_project_2026` 페이지에는 새로운 작업 기록을 추가하지 않는다.
- 이름이 비슷한 다른 페이지를 사용하지 않고, 반드시 상위 경로가 `VS Code > VOC_Improve`인지 확인한 뒤 데이터베이스를 업데이트한다.

## 기록 방식

- 완료 작업마다 `VOC 작업 기록` 데이터베이스에 항목 하나를 생성한다.
- 데이터베이스 속성은 작업명, 작업 일시, 메뉴, 유형, 상태, 중요도, 요약, 수정 파일, 검증 결과를 입력한다.
- 항목 본문의 프롬프트는 날짜·시간이 포함된 초록색 callout으로 작성한다.
- 항목 본문의 답변은 파란색 토글로 작성하고 수행 내용, 수정 파일, 검증 결과, 실행·확인 방법, 주의 사항과 완료 시간을 포함한다.
- `VOC_Improve` 인덱스에는 긴 작업 로그를 직접 누적하지 않고 현재 상태, 핵심 링크와 운영 원칙만 유지한다.
- 여러 기능에 장기적으로 영향을 주는 결정만 `중요 설계 결정`에 배경, 선택 방안, 대안, 영향 범위와 재검토 조건을 기록한다.
- 중요한 진단, 결정, 구현 변경과 검증 결과만 작업 완료 시 기록한다.
- 단순 조회와 중간 진행 상황은 기록하지 않는다.
- API 키, 토큰, 개인정보 등 비밀값은 파일이나 Notion에 기록하지 않는다.
- Notion 연결 또는 쓰기에 실패하면 실패 사실과 기록할 내용을 사용자에게 알린다.
