global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "ai-quality-api"
    metrics_path: "/metrics"
    static_configs:
      - targets:
          - "api:8000"

  - job_name: "qa-observer"
    metrics_path: "/metrics"
    static_configs:
      - targets:
          - "qa-observer:8010"
